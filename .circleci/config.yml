version: 2
jobs:
  build:
    docker:
      - image: nixorg/nix:circleci

    steps:
      - checkout
      - run:
          name: Linting
          command: nix-shell --option sandbox false --run "make lint"
      - run:
          name: Unit tests
          command: nix-shell --option sandbox false --run "make configure tests"
